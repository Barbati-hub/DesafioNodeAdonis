@layout('layouts/app')

@section('title')
{{ produto ? '✏️ Editar Produto' : '🛒 Cadastro de Produto' }}
@endsection

@section('content')
  <div class="bg-white p-6 shadow-lg rounded-lg">
    <h2 class="text-lg font-semibold text-gray-700">
      {{ produto ? '✏️ Editar Produto' : '🛒 Cadastro de Produto' }}
    </h2>
    <p class="text-gray-600 mt-2">
      {{ produto ? 'Altere os campos abaixo e salve as mudanças.' : 'Aqui você pode adicionar novos produtos ao sistema.' }}
    </p>

    <!-- Formulário de Produto -->
    <form action="{{ produto ? `/produtos/${produto.id}?_method=PUT` : '/produtos' }}" method="POST" class="mt-4">
      @if(produto)
        <input type="hidden" name="_method" value="PUT">
      @endif

      <div class="grid grid-cols-2 gap-4">
        <div>
          <label class="block text-gray-700 font-medium">Nome</label>
          <input type="text" name="nome" class="w-full p-3 border rounded-md" placeholder="Nome do produto" value="{{ produto?.nome ?? '' }}" required />
        </div>
        <div>
          <label class="block text-gray-700 font-medium">Descrição</label>
          <input type="text" name="descricao" class="w-full p-3 border rounded-md" placeholder="Descrição" value="{{ produto?.descricao ?? '' }}" />
        </div>
      </div>

      <div class="grid grid-cols-2 gap-4 mt-4">
        <div>
          <label class="block text-gray-700 font-medium">Preço (R$)</label>
          <input type="number" step="0.01" name="preco" class="w-full p-3 border rounded-md" placeholder="Preço" value="{{ produto?.preco ?? '' }}" required />
        </div>
        <div>
          <label class="block text-gray-700 font-medium">Quantidade</label>
          <input type="number" name="quantidade" class="w-full p-3 border rounded-md" placeholder="Quantidade" value="{{ produto?.quantidade ?? '' }}" required />
        </div>
      </div>

      <button type="submit" class="mt-4 bg-blue-700 hover:bg-blue-800 text-white px-5 py-2 rounded-md transition">
        {{ produto ? '💾 Salvar Alterações' : '➕ Adicionar Produto' }}
      </button>
    </form>
  </div>
@endsection
